buildCommand: cd backend && pip install -r requirements.txt
startCommand: cd backend && uvicorn main_prod:app --host 0.0.0.0 --port $PORT
env:
  - key: PORT
    value: 10000
  - key: MONGODB_URI
    sync: false